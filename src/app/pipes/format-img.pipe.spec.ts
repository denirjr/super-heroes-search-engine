import { FormatImgPipe } from './format-img.pipe';

describe('FormatImgPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatImgPipe();
    expect(pipe).toBeTruthy();
  });
});
